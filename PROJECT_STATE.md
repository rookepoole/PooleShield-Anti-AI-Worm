# PooleShield Project State

Version: 3.6.1

Current milestone: v3.6.1 fixes the GitHub Actions CI bootstrap by installing pytest before running the test suite, while preserving the v3.6 repo safety checks.

Completed: deterministic ChatGPT DAT archive pass, metadata rollup dashboard, read-only file/folder AV scanner, file AV review ledger, trusted hash baseline, baseline-aware file AV scan, local rule packs, archive-aware baseline merge, final scan summaries, and CI safety checks.

Privacy boundary remains intact: raw logs, decoded DAT text, normalized event JSONL, local review evidence, raw scanned file contents, baseline JSON, and private Poole Math / Poole Manifold / Poole Defect Calculus IP are not included in privacy bundles or public repo commits.

Next: push v3.6.1 if CI passes, then begin v3.7 configuration system.
